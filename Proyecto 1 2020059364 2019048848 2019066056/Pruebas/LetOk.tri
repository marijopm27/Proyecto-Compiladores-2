!  lets anidados 
let
  const a ~ 1 ! debe inferir que a : Integer
in

let
    const b ~ a ; ! es visible aquí
    var   a : Integer  ! debe inferir que nueva a : Integer
in 
    putint (a) ! debe imprimir 2
  end

end
 
